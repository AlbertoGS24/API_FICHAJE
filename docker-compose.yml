services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: fichar
      POSTGRES_PASSWORD: fichar
      POSTGRES_DB: fichar
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata: